[33mdbc2c93[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m, [m[1;31morigin/main[m[33m, [m[1;31morigin/HEAD[m[33m)[m Revert "feat: suporte a múltiplas contas do Instagram  Antes o app só operava com uma conta por vez (tabela `config` de linha única), então automações de uma segunda conta conectada tentavam enviar usando o token da outra e falhavam silenciosamente.  - nova tabela `accounts` (uma linha por conta conectada), substitui `config` - automations/contacts/queue passam a ter `account_id` - oauth/callback faz upsert por ig_user_id em vez de sobrescrever a   config única (permite conectar 2+ contas sem uma derrubar a outra) - webhook resolve a conta pelo entry.id e só processa automações   dessa conta - cron/drain envia usando o token/ig_user_id da conta de cada item   da fila - cron/refresh-token renova o token de cada conta conectada - nova rota GET/DELETE /api/accounts - media, automations, stats e events passam a filtrar por account_id - dashboard ganha seletor de contas (pills) + botão "conectar outra   conta"; new-automation herda a conta certa ao criar/editar  Migração necessária: rodar o bloco "MIGRAÇÃO: múltiplas contas de Instagram" no final de supabase/schema.sql (idempotente, seguro rodar de novo)."
[33m2d53259[m feat: suporte a múltiplas contas do Instagram  Antes o app só operava com uma conta por vez (tabela `config` de linha única), então automações de uma segunda conta conectada tentavam enviar usando o token da outra e falhavam silenciosamente.  - nova tabela `accounts` (uma linha por conta conectada), substitui `config` - automations/contacts/queue passam a ter `account_id` - oauth/callback faz upsert por ig_user_id em vez de sobrescrever a   config única (permite conectar 2+ contas sem uma derrubar a outra) - webhook resolve a conta pelo entry.id e só processa automações   dessa conta - cron/drain envia usando o token/ig_user_id da conta de cada item   da fila - cron/refresh-token renova o token de cada conta conectada - nova rota GET/DELETE /api/accounts - media, automations, stats e events passam a filtrar por account_id - dashboard ganha seletor de contas (pills) + botão "conectar outra   conta"; new-automation herda a conta certa ao criar/editar  Migração necessária: rodar o bloco "MIGRAÇÃO: múltiplas contas de Instagram" no final de supabase/schema.sql (idempotente, seguro rodar de novo).
[33mbfa4800[m Revert "feat: suporte a múltiplas contas do Instagram  Antes o app só operava com uma conta por vez (tabela `config` de linha única), então automações de uma segunda conta conectada tentavam enviar usando o token da outra e falhavam silenciosamente.  - nova tabela `accounts` (uma linha por conta conectada), substitui `config` - automations/contacts/queue passam a ter `account_id` - oauth/callback faz upsert por ig_user_id em vez de sobrescrever a   config única (permite conectar 2+ contas sem uma derrubar a outra) - webhook resolve a conta pelo entry.id e só processa automações   dessa conta - cron/drain envia usando o token/ig_user_id da conta de cada item   da fila - cron/refresh-token renova o token de cada conta conectada - nova rota GET/DELETE /api/accounts - media, automations, stats e events passam a filtrar por account_id - dashboard ganha seletor de contas (pills) + botão "conectar outra   conta"; new-automation herda a conta certa ao criar/editar  Migração necessária: rodar o bloco "MIGRAÇÃO: múltiplas contas de Instagram" no final de supabase/schema.sql (idempotente, seguro rodar de novo)."
[33m55424ce[m feat: suporte a múltiplas contas do Instagram  Antes o app só operava com uma conta por vez (tabela `config` de linha única), então automações de uma segunda conta conectada tentavam enviar usando o token da outra e falhavam silenciosamente.  - nova tabela `accounts` (uma linha por conta conectada), substitui `config` - automations/contacts/queue passam a ter `account_id` - oauth/callback faz upsert por ig_user_id em vez de sobrescrever a   config única (permite conectar 2+ contas sem uma derrubar a outra) - webhook resolve a conta pelo entry.id e só processa automações   dessa conta - cron/drain envia usando o token/ig_user_id da conta de cada item   da fila - cron/refresh-token renova o token de cada conta conectada - nova rota GET/DELETE /api/accounts - media, automations, stats e events passam a filtrar por account_id - dashboard ganha seletor de contas (pills) + botão "conectar outra   conta"; new-automation herda a conta certa ao criar/editar  Migração necessária: rodar o bloco "MIGRAÇÃO: múltiplas contas de Instagram" no final de supabase/schema.sql (idempotente, seguro rodar de novo).
[33m2d2ad08[m fix: corrigir ramificação e processamento dos cliques
